<h1>Resultado de la busqueda</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Correo Electronico</th>
  </tr>

<% @usuarios.each do |usuario| %>
  <tr>

    <td><%= link_to usuario.id, paciente_path(usuario.id) %></td>
   
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Agregar Doctor', new_doctor_path %> |
<%= link_to 'Atras', clinica_path %>
